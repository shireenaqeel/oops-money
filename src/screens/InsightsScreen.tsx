// InsightsScreen.tsx — 7-day bar, 6-month trend, category breakdown (built in Feature 6).
import React from 'react';
import { Screen, Placeholder } from '../components/shared';

export default function InsightsScreen() {
  return (
    <Screen>
      <Placeholder emoji="✿" title="Insights" subtitle="charts aur spending patterns yahan dikhenge ✨" />
    </Screen>
  );
}
