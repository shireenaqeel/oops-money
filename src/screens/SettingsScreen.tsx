// SettingsScreen.tsx — budget, income, splurge fund, app preferences (built out across later features).
import React from 'react';
import { Screen, Placeholder } from '../components/shared';

export default function SettingsScreen() {
  return (
    <Screen>
      <Placeholder emoji="🎀" title="Settings" subtitle="budget, income aur app settings yahan aayenge ✨" />
    </Screen>
  );
}
